name: Hello Worldd

on:
  push:
   branches:
    - 'main'
   pull_request:
    branches:
     - 'main'

jobs:
  hello:
   runs-on: linux

   steps:
    - name: Hello Step
      run: echo "Hello World"
